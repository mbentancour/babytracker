# During setup mode, hostapd manages wlan0 to provide the captive portal AP.
# NetworkManager must not touch wlan0 until setup-wifi.sh removes this file.
[keyfile]
unmanaged-devices=interface-name:wlan0
